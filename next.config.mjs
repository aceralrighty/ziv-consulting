/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: ['ziv-consulting.pages.dev']
    },
    experimental: {
        appDir: true
    }

};


export default nextConfig;
